name: gas_user_app
description: Commercial LPG Gas Delivery Application (Customer App)

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.5.0
  firebase_messaging: ^15.1.0
  firebase_app_check: ^0.3.1+6


  # State
  provider: ^6.1.2
  shared_preferences: ^2.2.2

  # Utils
  intl: ^0.19.0
  cupertino_icons: ^1.0.6

  # Razorpay
  razorpay_flutter: ^1.3.7
  
  path_provider: ^2.1.1
  share_plus: ^10.1.2
  pdf: ^3.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
